 <% if obj.errors.any? %>
  <div class="row">
    <div class="alert alert-danger col-xs-8 col-xs-offset-2">
    
    <h3>
      <%= pluralize(obj.errors.count, "error") %> prenvented the article from being saved
        
     </h3>
    <ul>
      <% obj.errors.full_messages.each do |msg| %>  
      <li><%= msg %></li>
      <% end %>
    </ul>
    <% end %>
    </div>
  </div>